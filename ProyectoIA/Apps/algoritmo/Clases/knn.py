import math
import numpy
from scipy.spatial import distance


def knn(vectorDatos, datoPrueba, k):
    individuos = []
    for i in range(len(vector.items())):
        individuos.append({'distancia': 0})





knn(vector, [1, 3, 7], 3)
a = numpy.array((1, 3, 7))
b = numpy.array((-1, -1, 0))
a = [1, 3, 7]
b = [-1, 0, 2]
dist = distance.euclidean(a, b)
print("--------------")
print(dist)
